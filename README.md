# AQL Service

A public service that allows executing `AQL queries` from clients, 
providing the necessary level of abstraction and security.

## Installation

```bash
composer require aql/service
```

## Features